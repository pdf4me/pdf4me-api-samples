service: classify-document-lambda

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1
  environment:
    PDF4ME_API_KEY: ${env:PDF4ME_API_KEY}
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - s3:GetObject
            - s3:PutObject
            - s3:DeleteObject
          Resource: 
            - "arn:aws:s3:::classifydocument-input-bucket/*"
            - "arn:aws:s3:::classifydocument-output-bucket/*"

functions:
  classifyDocument:
    handler: classify_document_lambda.handler
    events:
      - httpApi:
          path: /classify-document
          method: POST
      - s3:
          bucket: classifydocument-input-bucket
          event: s3:ObjectCreated:*
          existing: true
    environment:
      OUTPUT_BUCKET: classifydocument-output-bucket

resources:
  Resources:
    ClassifyDocumentInputBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: classifydocument-input-bucket
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true

    ClassifyDocumentOutputBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: classifydocument-output-bucket
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true

plugins:
  - serverless-offline 