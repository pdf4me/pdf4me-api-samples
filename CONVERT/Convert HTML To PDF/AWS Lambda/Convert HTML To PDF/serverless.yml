service: convert-html-to-pdf-lambda

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1
  environment:
    PDF4ME_API_KEY: ${env:PDF4ME_API_KEY}
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - s3:GetObject
            - s3:PutObject
            - s3:DeleteObject
          Resource: 
            - "arn:aws:s3:::html-to-pdf-input-bucket/*"
            - "arn:aws:s3:::html-to-pdf-output-bucket/*"

functions:
  convertHtmlToPdf:
    handler: convert_html_to_pdf_lambda.handler
    events:
      - httpApi:
          path: /convert-html-to-pdf
          method: POST
      - s3:
          bucket: html-to-pdf-input-bucket
          event: s3:ObjectCreated:*
          existing: true
    environment:
      OUTPUT_BUCKET: html-to-pdf-output-bucket

resources:
  Resources:
    HtmlToPdfInputBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: html-to-pdf-input-bucket
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true

    HtmlToPdfOutputBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: html-to-pdf-output-bucket
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true

plugins:
  - serverless-offline 