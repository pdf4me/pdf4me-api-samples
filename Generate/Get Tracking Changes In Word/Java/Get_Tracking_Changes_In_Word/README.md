# Get Tracking Changes In Word - Java Prototype

This project demonstrates how to get tracking changes from a Word document using the PDF4ME API in Java.

## Usage

- Place your input DOCX as `sample.docx` in this directory.
- Run `Main.java` to extract tracking changes as JSON.

## TODO
- Implement the logic in `src/Main.java` following the C# Program.cs logic. 