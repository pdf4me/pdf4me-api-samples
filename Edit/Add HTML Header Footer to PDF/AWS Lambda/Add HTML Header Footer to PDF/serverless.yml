service: pdf4me-add-html-header-footer-lambda

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1
  memorySize: 512
  timeout: 300
  environment:
    PDF4ME_API_KEY: ${env:PDF4ME_API_KEY}
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - s3:GetObject
            - s3:PutObject
            - s3:DeleteObject
          Resource:
            - arn:aws:s3:::pdf-html-header-footer-input-bucket/*
            - arn:aws:s3:::pdf-html-header-footer-output-bucket/*

functions:
  addHtmlHeaderFooter:
    handler: add_html_header_footer_lambda.handler
    events:
      - httpApi:
          path: /add-html-header-footer
          method: POST
      - s3:
          bucket: pdf-html-header-footer-input-bucket
          event: s3:ObjectCreated:*
          rules:
            - suffix: .pdf
          existing: true

resources:
  Resources:
    InputBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: pdf-html-header-footer-input-bucket
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true

    OutputBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: pdf-html-header-footer-output-bucket
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true

plugins:
  - serverless-offline 