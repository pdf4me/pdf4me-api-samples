service: add-margin-to-pdf-lambda

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1
  environment:
    PDF4ME_API_KEY: ${env:PDF4ME_API_KEY}
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - s3:GetObject
            - s3:PutObject
            - s3:DeleteObject
          Resource: 
            - "arn:aws:s3:::pdf-margin-input-bucket/*"
            - "arn:aws:s3:::pdf-margin-output-bucket/*"

functions:
  addMarginToPdf:
    handler: add_margin_to_pdf_lambda.handler
    events:
      - httpApi:
          path: /add-margin-to-pdf
          method: POST
      - s3:
          bucket: pdf-margin-input-bucket
          event: s3:ObjectCreated:*
          existing: true
    environment:
      OUTPUT_BUCKET: pdf-margin-output-bucket

resources:
  Resources:
    PdfMarginInputBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: pdf-margin-input-bucket
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true

    PdfMarginOutputBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: pdf-margin-output-bucket
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true

plugins:
  - serverless-offline 