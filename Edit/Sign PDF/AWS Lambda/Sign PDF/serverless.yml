service: sign-pdf-lambda

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1
  environment:
    PDF4ME_API_KEY: ${env:PDF4ME_API_KEY}
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - s3:GetObject
            - s3:PutObject
            - s3:DeleteObject
          Resource: 
            - "arn:aws:s3:::pdf-sign-input-bucket/*"
            - "arn:aws:s3:::pdf-sign-output-bucket/*"

functions:
  signPdf:
    handler: sign_pdf_lambda.handler
    events:
      - httpApi:
          path: /sign-pdf
          method: POST
      - s3:
          bucket: pdf-sign-input-bucket
          event: s3:ObjectCreated:*
          existing: true
    environment:
      OUTPUT_BUCKET: pdf-sign-output-bucket

resources:
  Resources:
    PdfSignInputBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: pdf-sign-input-bucket
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true

    PdfSignOutputBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: pdf-sign-output-bucket
        PublicAccessBlockConfiguration:
          BlockPublicAcls: true
          BlockPublicPolicy: true
          IgnorePublicAcls: true
          RestrictPublicBuckets: true

plugins:
  - serverless-offline 