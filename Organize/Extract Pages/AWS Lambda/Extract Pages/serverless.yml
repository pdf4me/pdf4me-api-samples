service: pdf4me-extract-pages-lambda

frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1
  environment:
    PDF4ME_API_KEY: ${env:PDF4ME_API_KEY}
  timeout: 300
  memorySize: 512
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - s3:GetObject
            - s3:PutObject
            - s3:DeleteObject
          Resource: 
            - "arn:aws:s3:::pdf-extract-pages-input-bucket/*"
            - "arn:aws:s3:::pdf-extract-pages-output-bucket/*"

functions:
  extractPages:
    handler: extract_pages_lambda.handler
    events:
      - httpApi:
          path: /extract-pages
          method: POST
      - s3:
          bucket: pdf-extract-pages-input-bucket
          event: s3:ObjectCreated:*
          rules:
            - suffix: .pdf
          existing: true

package:
  patterns:
    - '!node_modules/.prisma/**'
    - '!node_modules/@prisma/engines/**'
    - '!node_modules/prisma/**'
    - '!tests/**'
    - '!test/**'
    - '!*.test.js'
    - '!*.spec.js'
    - '!.git/**'
    - '!.vscode/**'
    - '!README.md'
    - '!serverless.yml'
    - '!package.json'
    - '!package-lock.json' 